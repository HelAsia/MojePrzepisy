<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto"
  xmlns:tools="http://schemas.android.com/tools"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  tools:context=".log_out.LogoutActivityView"
  android:background="@color/primary_light">

  <ScrollView
    android:id="@+id/scrollViewLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <RelativeLayout
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:layout_below="@+id/scrollViewLayout">

      <ImageView
        android:id="@+id/logout_logo"
        android:layout_width="wrap_content"
        android:layout_height="428dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        app:srcCompat="@drawable/logo"/>

      <TextView
        android:id="@+id/logout_question_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="@string/logout_question"
        android:layout_below="@+id/logout_logo"
        android:textSize="@dimen/edit_text_size"/>

      <Button
        android:id="@+id/logout_button_yes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="90dp"
        android:layout_alignTop="@+id/logout_button_no"
        android:layout_below="@+id/logout_question_tv"
        android:text="@string/yes"/>
      <Button
        android:id="@+id/logout_button_no"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="90dp"
        android:layout_alignParentEnd="true"
        android:layout_below="@+id/logout_question_tv"
        android:text="@string/no"/>

      <TextView
        android:id="@+id/textViewErrorMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="62dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_below="@+id/logout_button_no"
        android:text=""/>

    </RelativeLayout>
  </ScrollView>
</RelativeLayout>